# FAgent 项目开发规则

## Python 虚拟环境

- 虚拟环境名称：`fg`
- 虚拟环境路径：项目根目录下的 `.venv`
- Python 版本：3.12
- 所有 Python 相关操作（安装包、运行测试、启动服务）都必须在此虚拟环境中执行

### 使用规则

1. 执行 Python 命令前，确保激活虚拟环境：
   ```bash
   # 在项目根目录执行
   source .venv/bin/activate
   ```

2. 安装新包时：
   - 在虚拟环境中使用 `pip install`
   - 更新对应的 `requirements.txt`（backend 或 tests）

3. 运行测试或服务时：
   - 后端服务：`cd backend && ./run.sh` 或 `uvicorn api.main:app --reload`
   - Streamlit 测试：`cd tests && streamlit run streamlit_test.py`

## 项目结构

- `backend/` - 后端 Python 服务
- `tests/` - 测试代码和 Streamlit 测试界面
- `docs/changelog/` - 变更记录（本地）
- `docs/REQUIREMENTS_LOG.md` - 需求记录（本地）

## 代码规范

- Python 使用 Python 3.12
- 优先使用类型注解
- 文档使用中文
- API 路径以 `/api/` 开头，流式用 SSE，非流式用 RESTful

## 文档规范

- 文档中避免硬编码路径、用户名等环境相关信息
- 使用占位符或变量表示，如 `{workspace}`、`{project_root}`、`<your_api_key>`
- 便于团队成员在不同环境下协作开发

## Git 提交规则

### Commit Message 格式（英文）

```
<type>(<scope>): <short description>
```

**Type（必选）：**
- `feat` - 新功能
- `fix` - Bug 修复
- `docs` - 文档更新
- `refactor` - 重构（非新功能/修复）
- `test` - 测试相关
- `chore` - 构建/依赖/配置

**Scope（可选）：** `api`, `llm`, `storage`, `test`, `config`

**示例：**
```bash
feat(api): add multi-turn conversation support
fix(storage): fix message history filter logic
refactor(llm): extract LLM client to separate module
docs: update architecture documentation
chore: update dependencies
```

### Commit 前检查

**1. 敏感信息检查**

检查项目：
- API Key：不能包含 `sk-or-v1-`、`sk-proj-` 等真实密钥
- 密码/密钥：不能硬编码
- 个人信息：不能包含真实邮箱、私人路径

检查命令：
```bash
git diff --cached | grep -iE "sk-or-v1-|sk-proj-|password=|secret="
```

**2. 文档同步检查**

代码变更后，检查以下文档是否需要更新：
- `README.md` - 项目说明（结构、启动命令）
- `backend/README.md` - 后端说明
- `backend/API_USAGE.md` - API 使用文档
- `backend/ARCHITECTURE.md` - 架构文档

**已忽略的文件（`.gitignore`）：**
- `.env`、`data/`、`.venv/`、`logs/`
- `docs/changelog/`、`docs/REQUIREMENTS_LOG.md`

### Commit 后：更新本地记录

**每次成功 commit 后，执行以下两步：**

1. **更新 Changelog**
   - 路径：`docs/changelog/YYYYMMDD-模块名-简要说明.md`
   - 内容：变更模块、功能说明、技术选型

2. **更新需求记录**
   - 路径：`docs/REQUIREMENTS_LOG.md`
   - 格式：
     ```markdown
     ### HH:MM - 需求简述
     - 需求要点 1
     - 需求要点 2
     ```
   - 排序：日期倒序，同一天按时间倒序

> 注：这两个文件仅本地保留，不提交到 GitHub
