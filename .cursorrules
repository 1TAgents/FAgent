# FAgent 项目开发规则

## Python 虚拟环境

- 虚拟环境名称：`fg`
- 虚拟环境路径：项目根目录下的 `.venv`
- Python 版本：3.12
- 所有 Python 相关操作（安装包、运行测试、启动服务）都必须在此虚拟环境中执行

### 使用规则

1. 执行 Python 命令前，确保激活虚拟环境：
   ```bash
   source /Users/hayden/Desktop/Hayden/00_project/FAgent/.venv/bin/activate
   ```

2. 安装新包时：
   - 在虚拟环境中使用 `pip install`
   - 更新对应的 `requirements.txt`（backend 或 tests）

3. 运行测试或服务时：
   - 后端服务：`cd backend && ./run.sh` 或 `uvicorn api.main:app --reload`
   - Streamlit 测试：`cd tests && streamlit run streamlit_test.py`

## 项目结构

- `backend/` - 后端 Python 服务
- `tests/` - 测试代码和 Streamlit 测试界面
- `docs/changelog/` - 变更记录
- `docs/REQUIREMENTS_LOG.md` - 需求记录

## 需求记录规则

**重要**：每次用户提出新的开发需求后，必须更新需求记录文档。

- 文档路径：`docs/REQUIREMENTS_LOG.md`
- 记录格式：
  ```markdown
  ### HH:MM - 需求简述
  - 需求要点 1
  - 需求要点 2
  ```
- 排序规则：
  - 日期倒序（最新日期在上）
  - 同一天的需求按时间倒序（最新需求在上）
- 更新时机：完成需求开发后，将需求要点记录到文档

## 代码规范

- Python 使用 Python 3.12
- 优先使用类型注解
- 文档使用中文
- API 路径以 `/api/` 开头，流式用 SSE，非流式用 RESTful

## Git 提交规则

### Commit Message 格式（英文）

```
<type>(<scope>): <short description>
```

**Type（必选）：**
- `feat` - 新功能
- `fix` - Bug 修复
- `docs` - 文档更新
- `refactor` - 重构（非新功能/修复）
- `test` - 测试相关
- `chore` - 构建/依赖/配置

**Scope（可选）：** `api`, `llm`, `storage`, `test`, `config`

**示例：**
```bash
feat(api): add multi-turn conversation support
fix(storage): fix message history filter logic
refactor(llm): extract LLM client to separate module
docs: update architecture documentation
chore: update dependencies
```

### 敏感信息检查

**重要**：每次 `git commit` 前必须检查敏感信息。

#### 检查项目

1. **API Key**：不能包含 `sk-or-v1-`、`sk-proj-` 等真实密钥
2. **密码/密钥**：不能硬编码密码、secret、token
3. **个人信息**：不能包含真实邮箱（示例邮箱除外）、私人路径

### 检查命令

```bash
# 检查暂存区是否有敏感信息
git diff --cached | grep -iE "sk-or-v1-|sk-proj-|password=|secret="
```

### 已忽略的敏感文件

确保以下文件/目录在 `.gitignore` 中：
- `.env` - 环境变量
- `data/` - 数据库文件
- `.venv/` - 虚拟环境
- `docs/changelog/` - 变更记录
- `docs/REQUIREMENTS_LOG.md` - 需求记录
