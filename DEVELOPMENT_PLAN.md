# FAgent 开发路线图

> 更新日期：2026-04-20

本文档只保留当前主线的开发状态和下一步方向，不再追踪历史上已经失效的阶段拆分。

## 当前状态

| 模块 | 状态 | 说明 |
|------|------|------|
| Frontend | 可运行 | Web 聊天界面、会话侧边栏、认证弹窗、模型选择 |
| Backend | 可运行 | 会话/消息存储、认证、SSE、模型列表代理 |
| Agents | 可运行 | Router、行情工具、标题生成、Mock/真实 LLM |
| Memory | 基础能力已落地 | 存储、查询 API、layers、测试已在仓库中 |
| CLI | 部分完成 | 框架和 session 命令可用，其余命令仍需补齐 |
| Data Sync | 可选实验服务 | 用于慢速预热历史数据缓存 |

## 已完成

- 三层服务拆分：Frontend / Backend / Agents
- 会话创建、历史拉取、重命名、删除
- SSE 流式对话链路
- 用户注册、登录、JWT 鉴权
- 会话标题自动生成
- 行情工具接口与缓存接口
- 模型列表动态获取
- Memory 的 ID、模型、SQLite 存储、查询 API、基础测试

## 正在整理

- 文档与依赖声明对齐
- Memory/CLI 的功能边界梳理
- 主 README 与各模块 README 同步

## 下一步

1. 补齐 CLI 的 `message` / `memory` / `test` 命令真实实现。
2. 把 Memory 查询和三层记忆逐步接入 Backend / Agents，而不只是停留在独立模块。
3. 整理 `modules/` 与主聊天链路之间的关系，避免实验模块和线上链路混淆。
4. 为前后端增加更稳定的自动化冒烟测试与 CI。
5. 继续完善部署文档、生产代理和配置约束。

## 备注

- Android 文档保留为未来规划，不是当前交付目标。
- 历史测试/完成报告继续保留，但不再作为当前状态依据。
