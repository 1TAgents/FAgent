---
description: Git commit convention and pre-commit security checks
globs:
alwaysApply: true
---

# Git Commit Convention

## Format
```
<type>(<scope>): <subject>
```

## Types

| Type | Usage |
|------|-------|
| **feat** | New feature |
| **fix** | Bug fix |
| **docs** | Documentation only |
| **refactor** | Code refactoring (no feature change) |
| **perf** | Performance improvement |
| **style** | Code style (formatting, no logic change) |
| **test** | Adding or updating tests |
| **chore** | Build process or auxiliary tools |
| **ci** | CI configuration |

## Rules

1. **type is required**, scope is optional
2. **Use imperative mood**: `add` not `added`
3. **Lowercase first letter**, no period at the end
4. **Subject length**: 50 chars ideal, 72 chars max

---

# Pre-Commit Security Checks

在执行 `git add` 或 `git commit` 之前，必须检查以下敏感信息：

## 必检项

1. **API Key / Secret**
   - 匹配模式：`sk-`、`api_key`、`secret`、`token`、`password` 等
   - `.env` 文件绝不提交，只提交 `.env.example`（仅含占位符）

2. **用户名 / 邮箱 / 姓名**
   - 检查文件内容中是否包含真实用户名、邮箱地址或个人姓名
   - 配置文件中的示例值应使用占位符

3. **绝对路径**
   - 匹配模式：`/Users/`、`/home/`、`C:\Users\` 等
   - 代码和配置中不应出现本机绝对路径

4. **私有域名 / 内网地址**
   - 检查是否包含内网 IP（`192.168.x.x`、`10.x.x.x`）或私有域名

## 检查方式

对待提交的文件执行以下搜索，确认无匹配后再提交：

```bash
git diff --cached --diff-filter=ACMR | grep -iE "sk-[a-zA-Z0-9]{10,}|api_key\s*=|secret\s*=|password\s*=|/Users/|/home/|C:\\\\Users"
```

如发现匹配，必须清理后再提交，或确认该内容属于安全的占位符/示例值。
